# Public Benchmark v1.1 Results Summary

The released `v1.1` benchmark waves now contribute:

- `4` new baseline excerpts;
- `24` controlled variants;
- `4` benchmark languages (`de`, `ru`, `en`, `fr`);
- `2` released source classes.

## Current usefulness

This release is useful because it:

- adds the first German-language released baseline to the public benchmark program;
- adds the first Russian-language released baseline to the public benchmark program;
- adds an English government prose baseline rather than extending literary prose only;
- adds a French literary baseline to reduce language concentration;
- preserves the six perturbation axes already used in the current released benchmark subset;
- creates a clean bridge from intake planning to a non-zero released `v1.1` increment.

## Guardrail

This is still a small benchmark-expansion release. It does not by itself justify broader benchmark-performance claims or stronger inferential wording.
