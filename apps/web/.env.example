# CogniPrint Content Scanner Web — Environment Variables
# Copy this file to .env.local for local development.
# In Vercel, set these variables via the Vercel dashboard (never commit real values).

# ── API ───────────────────────────────────────────────────────────────────────
# Public URL of the Railway backend (no trailing slash).
VITE_API_BASE_URL=https://api.cogniprint.ai
