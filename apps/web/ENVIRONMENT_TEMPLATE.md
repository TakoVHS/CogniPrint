# Frontend Environment Variables

Set these variables in Vercel:

- VITE_API_BASE_URL

The backend identified by `VITE_API_BASE_URL` is expected to serve:

- `GET /ready`
- `GET /account/status`
- `POST /scan`
