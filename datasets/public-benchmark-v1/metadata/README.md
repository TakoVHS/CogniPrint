# Metadata Notes

This directory is reserved for benchmark provenance and license records.

Each released sample in `public-benchmark-v1` should have:

- `sample_id`
- relation type (`baseline` or `variant`)
- baseline sample reference when applicable
- license
- source URL
- acquisition date
- source class
- language
- usage note

Do not add benchmark samples here without clear provenance and licensing.
